import { Routes } from '@angular/router';
import { ResetPasswordComponent } from './resetpassword.component';




export const ResetPasswordRoutes: Routes = [
  { path: '', component: ResetPasswordComponent, data: { title: 'resetpassword' } },
  
];

